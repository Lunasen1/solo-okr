# 贡献指南

感谢你想为独行OKR 贡献！这是一个「个人目标管理」工具，欢迎任何形式的参与。

## 如何贡献

1. Fork 本仓库
2. 在本地修改 `index.html`
3. 提交 Pull Request，描述你改了什么、为什么

## 贡献时的注意事项

- **保持单文件、零依赖**：不引入第三方库 / CDN，动效一律用原生 CSS / JS
- **数据结构兼容**：不要改 localStorage 的 KEY 和数据结构；确需改动时，必须附带旧数据迁移逻辑（参考现有 `LEGACY_KEY` 的迁移写法）
- **中文界面**：所有面向用户的文案用简体中文
- **设计调性**：清新、克制、不花哨——这是本项目的视觉灵魂

## 提交建议

- 一个 PR 只做一件事
- 改动大时先在 Issue 里讨论方向，避免白做
