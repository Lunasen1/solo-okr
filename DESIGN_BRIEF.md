# OKR 工具前端重设计 · 设计需求书

## 任务
把 `index.html`（单文件 HTML，主逻辑约 37KB）的**前端视觉 / 交互 / 动画层**彻底重做，达到「顶级、未来感、清新」水准。**功能逻辑必须原样保留**，一个都不能少、一个都不能坏。

文件里有一行 `<!--__XLSX_LIB__-->` 占位符，**不要动它**（交付后会重新内联 Excel 库）。

## 必须保留的现有功能（逐一核对，缺一不可）
1. 四个页签导航：本月目标 / 周复盘 / 历史归档 / 设置
2. 本月目标页：两个 O 卡片（成长型 / 业绩型），每个 O 下多个 KR；每个 KR 有：标题、单位、当前值、目标值、完成度进度、信心指数(0~10)、本周分(0~1)
3. 交互：编辑 O 标题、添加/删除 KR、改当前值/目标值/单位、调信心指数、调本周分、删除
4. 周复盘页：本周所有 KR 打分(0~1) + 三句话（做了什么/卡在哪/下周一个关键动作）+ 历史周复盘列表
5. 历史归档页：已归档周期列表，可展开看当时目标
6. 设置页：JSON 导入导出、Excel 导入导出（函数 exportExcel/importExcel 保留）、月末归档开新周期、清空
7. 悬浮「?」解释气泡（三个概念：目标分/信心指数/本周分）
8. 本地存储：localStorage KEY = `lp_okr_v1`，**数据结构绝对不能变**（用户已有数据必须兼容显示）

## 视觉方向：清新未来（Fresh Futurism）
一句话：像「清晨的太空站 / 未来实验室」——干净、轻盈、呼吸感、安静的高级。

- **配色**：浅色为主，**单一主色系**（清新蓝或青，低饱和），辅助色 ≤2 个且都低饱和。
  明令禁止：霓虹多色、高饱和撞色、暗黑赛博、五彩斑斓、眼花缭乱。
- **背景**：极浅渐变（如 白 → 极浅蓝/青），可加极淡的漂浮光晕或粒子，但要克制到几乎察觉不到。
- **材质**：毛玻璃卡片（backdrop-filter: blur）、半透明白、柔和阴影、大圆角，通透轻盈。
- **动效**：细腻的 spring 弹性过渡、数字滚动/计数动画、进度条流光、卡片悬浮微抬升。
  明令禁止：闪烁、glitch、扫描线、剧烈位移等花哨动效。动效要「让人舒服」，不是「让人兴奋到眼花」。
- **排版**：现代无衬线、大标题、充足留白、数字用 tabular-nums 等宽。
- **参考调性**：Apple visionOS 的玻璃美学、Linear、Arc、Notion AI 那种安静的高级感。

## 交互创新（重点，摆脱「表单感」）
现在的界面是纯表单式的（一堆输入框），太普通。要重新设计**核心动作**的交互，让人有「打开新世界大门」的感觉，同时保持清新克制：
- KR 完成度：考虑用**环形进度 + 鼠标在环上拖拽**直接调当前值（而不是敲输入框），或等价的直观手势。
- 数字变化：**滚动/计数动画**。
- 信心指数：精致滑杆，光标跟随有柔和光效。
- 周复盘打分：滑块 + 实时分数预览。
- 页面切换：流畅的转场过渡。
- 顶部周期概览：做成有「仪表感」但克制的总览（剩余天数、综合分）。
以上是方向建议，你有更好的「清新未来」交互方案可自行发挥，但务必保证：**功能等价 + 操作直观 + 不花哨**。

## 硬约束
- 保持**单文件** `index.html`，离线可用，中文界面。
- **不要引入任何第三方库/CDN**（动效全部用原生 CSS + 少量原生 JS 实现）。
- localStorage KEY 和数据结构不变；所有现有 JS 函数的功能语义必须等价（代码可重构）。
- 桌面浏览器为主（macOS Chrome / Safari）。
- 保留 `<!--__XLSX_LIB__-->` 占位符原样不动。

## 验收标准（我会逐条检查）
1. 浏览器打开无 console 报错。
2. 所有功能可用：增删改 KR、打分、周复盘、归档、JSON/Excel 导入导出。
3. 视觉达到「清新未来、顶级」水准，不眼花。
4. 动画流畅、不卡顿、不刺眼。
5. 用户已有 localStorage 数据（lp_okr_v1）打开后正常显示。

## 交付
改完 `index.html` 后执行 `git add -A && git commit -m "redesign: fresh futuristic OKR UI"`。
